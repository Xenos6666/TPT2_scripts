:global int turbo.cycles
:global int turbo.cycles.max

:global int turbo.instcounter

execute("turbo exec V2.0 trigger")
execute(if(turbo.cycles < turbo.cycles.max && turbo.instcounter > 0, "turbo exec V2.0 trigger", ""))
execute(if(turbo.cycles < turbo.cycles.max && turbo.instcounter > 0, "turbo exec V2.0 trigger", ""))
stop(if(turbo.cycles < turbo.cycles.max && turbo.instcounter > 0, "turbo exec V2.0 loop temp", ""))
